---
import { mirrors, scenarios } from '../config/mirrors';

// 简单的 Markdown 渲染函数
function renderMarkdown(text: string): string {
  if (!text) return '';
  
  // 转义 HTML
  let html = text.replace(/</g, '&lt;').replace(/>/g, '&gt;');
  
  // 代码块
  html = html.replace(/```(\w*)\n([\s\S]*?)```/g, '<pre><code>$2</code></pre>');
  
  // 行内代码
  html = html.replace(/`([^`]+)`/g, '<code>$1</code>');
  
  // 标题
  html = html.replace(/^### (.+)$/gm, '<h3>$1</h3>');
  html = html.replace(/^## (.+)$/gm, '<h2>$1</h2>');
  
  // 粗体
  html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
  
  // 列表
  html = html.replace(/^- (.+)$/gm, '<li>$1</li>');
  html = html.replace(/(<li>.*<\/li>)/s, '<ul>$1</ul>');
  
  // 分割线
  html = html.replace(/^---$/gm, '<hr>');
  
  // 换行
  html = html.replace(/\n\n/g, '</p><p>');
  html = html.replace(/\n/g, '<br>');
  
  return '<p>' + html + '</p>';
}
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>爪子星球 - OpenClaw 发行版</title>
    <meta name="description" content="爪子星球 - 极客风格的 OpenClaw 多能力集合门户" />
    <style>
      @import url('https://fonts.googleapis.com/css2?family=VT323&display=swap');
      
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body {
        font-family: 'VT323', 'Courier New', monospace;
        background: #0d1117;
        color: #c9d1d9;
        min-height: 100vh;
        font-size: 18px;
        line-height: 1.4;
      }
      a { color: #58a6ff; text-decoration: none; }
      a:hover { text-decoration: underline; }
      .container { max-width: 1100px; margin: 0 auto; padding: 1rem; }
      
      /* Header */
      header { text-align: center; padding: 1rem 0; border-bottom: 1px solid #30363d; }
      .logo-img { width: 300px; height: auto; }
      .tagline { color: #8b949e; font-size: 1rem; margin-top: 0.3rem; }
      .ascii-header { color: #238636; font-size: 0.5rem; line-height: 1.15; white-space: pre; }
      
      /* Filters */
      .filters { display: flex; gap: 1rem; flex-wrap: wrap; margin: 1rem 0; padding: 0.8rem; background: #161b22; border: 1px solid #30363d; }
      .filter-group { display: flex; flex-direction: column; gap: 0.2rem; }
      .filter-group label { font-size: 0.8rem; color: #8b949e; text-transform: uppercase; }
      .filter-group select { padding: 0.3rem 0.6rem; background: #0d1117; border: 1px solid #30363d; color: #c9d1d9; font-family: inherit; cursor: pointer; font-size: 0.9rem; }
      .filter-group select:focus { outline: none; border-color: #58a6ff; }
      
      /* Grid */
      .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 0.8rem; margin-top: 1rem; }
      .card { background: #161b22; border: 1px solid #30363d; padding: 0.8rem; }
      .card:hover { border-color: #58a6ff; background: #1f2428; }
      .card-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.5rem; border-bottom: 1px dashed #30363d; padding-bottom: 0.3rem; }
      .card-title { font-size: 1rem; color: #58a6ff; }
      .card-badge { font-size: 0.75rem; padding: 0.1rem 0.3rem; border: 1px solid; }
      .badge-windows { border-color: #0078d4; color: #0078d4; }
      .badge-linux { border-color: #3fb950; color: #3fb950; }
      .badge-macos { border-color: #8b949e; color: #8b949e; }
      .card-meta { display: flex; gap: 0.8rem; font-size: 0.8rem; color: #8b949e; margin-bottom: 0.5rem; }
      .card-desc { font-size: 0.85rem; color: #c9d1d9; margin-bottom: 0.5rem; }
      .card-skills { display: flex; flex-wrap: wrap; gap: 0.3rem; margin-bottom: 0.5rem; }
      .skill-tag { font-size: 0.7rem; padding: 0.1rem 0.4rem; border: 1px solid #238636; color: #238636; }
      .download-btn { display: block; width: 100%; padding: 0.4rem; background: #238636; color: #fff; text-align: center; text-decoration: none; font-size: 0.9rem; }
      .download-btn:hover { background: #2ea043; text-decoration: none; }
      
      /* Footer */
      footer { text-align: center; padding: 1.5rem 0; margin-top: 2rem; border-top: 1px solid #30363d; color: #8b949e; font-size: 0.85rem; }
      footer a { color: #58a6ff; }
      
      /* Empty */
      .empty { text-align: center; padding: 2rem; color: #8b949e; border: 1px solid #30363d; background: #161b22; }
      
      /* News Section */
      .news-section { margin: 1rem 0; }
      .news-updated { font-size: 0.8rem; color: #8b949e; }
      .news-header { display: flex; align-items: center; gap: 0.5rem; font-size: 0.95rem; margin: 1rem 0 0.6rem; padding-bottom: 0.3rem; border-bottom: 2px solid; }
      .news-header.latest { border-color: #1f6feb; color: #1f6feb; }
      .news-header.hot { border-color: #da3633; color: #da3633; }
      .news-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 0.6rem; margin-bottom: 0.8rem; }
      .news-card { background: #161b22; border: 1px solid #30363d; padding: 0.6rem; text-decoration: none; display: block; }
      .news-card:hover { border-color: #58a6ff; }
      .news-title { font-size: 0.85rem; color: #c9d1d9; margin-bottom: 0.4rem; line-height: 1.4; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
      .news-meta { font-size: 0.7rem; color: #8b949e; display: flex; justify-content: space-between; }
      .news-source { color: #238636; }
      .news-time { color: #8b949e; }
      .news-loading { text-align: center; padding: 1rem; color: #8b949e; }
      .news-section-title { display: flex; align-items: center; justify-content: space-between; margin: 1rem 0 0.5rem; }
      .news-section-title h2 { font-size: 0.95rem; margin: 0; color: #238636; }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <pre class="ascii-header">
 ██████╗██╗   ██╗██████╗ ███████╗██████╗     ██████╗ ███████╗ ██████╗ 
██╔════╝╚██╗ ██╔╝██╔══██╗██╔════╝██╔══██╗    ██╔══██╗██╔════╝██╔════╝ 
██║      ╚████╔╝ ██████╔╝█████╗  ██████╔╝    ██████╔╝█████╗  ██║  ███╗
██║       ╚██╔╝  ██╔══██╗██╔══╝  ██╔══██╗    ██╔══██╗██╔══╝  ██║   ██║
╚██████╗   ██║   ██████╔╝███████╗██║  ██║    ██║  ██║███████╗╚██████╔╝
 ╚═════╝   ╚═╝   ╚═════╝ ╚══════╝╚═╝  ╚═╝    ╚═╝  ╚═╝╚══════╝ ╚═════╝</pre>
        <p class="tagline">OpenClaw 发行版 · 开机即用的智能助手</p>
      </header>

      <!-- AI News Section -->
      <div class="news-section">
        <div class="news-section-title">
          <h2>🤖 AI 资讯</h2>
          <span id="updatedTime" class="news-updated">更新时间: --</span>
        </div>
        
        <!-- Latest AI News -->
        <div class="news-header latest">
          <span>📰 最新 AI 消息</span>
          <span style="font-size: 0.8rem; color: #888;">今日更新</span>
        </div>
        <div class="news-grid" id="latestNews">
          <div class="news-loading">加载中...</div>
        </div>
        
        <!-- Hot AI News -->
        <div class="news-header hot">
          <span>🔥 最热 AI 消息</span>
          <span style="font-size: 0.8rem; color: #888;">热门排行</span>
        </div>
        <div class="news-grid" id="hotNews">
          <div class="news-loading">加载中...</div>
        </div>
      </div>

      <div class="filters">
        <div class="filter-group">
          <label>操作系统</label>
          <select id="osFilter">
            <option value="">全部</option>
            <option value="windows">Windows</option>
            <option value="linux">Linux</option>
            <option value="macos">macOS</option>
          </select>
        </div>
        <div class="filter-group">
          <label>硬件架构</label>
          <select id="archFilter">
            <option value="">全部</option>
            <option value="x64">x64 (Intel/AMD)</option>
            <option value="arm64">ARM64 (Apple Silicon)</option>
            <option value="arm32">ARM32 (Raspberry Pi)</option>
          </select>
        </div>
        <div class="filter-group">
          <label>使用场景</label>
          <select id="scenarioFilter">
            <option value="">全部</option>
            {scenarios.map(s => (
              <option value={s.id}>{s.name}</option>
            ))}
          </select>
        </div>
      </div>

      <div class="grid" id="mirrorGrid">
        {mirrors.map(mirror => (
          <div class="card" data-os={mirror.os} data-arch={mirror.arch} data-skills={mirror.skills.join(',')}>
            <div class="card-header">
              <span class="card-title">{mirror.name}</span>
              <span class={`card-badge badge-${mirror.os}`}>{mirror.os}</span>
            </div>
            <div class="card-meta">
              <span>📦 {mirror.version}</span>
              <span>💾 {mirror.size}</span>
              <span>🔧 {mirror.arch}</span>
            </div>
            <p class="card-desc">{mirror.description}</p>
            <div class="card-skills">
              {mirror.skills.map(skill => (
                <span class="skill-tag">{skill}</span>
              ))}
            </div>
            {mirror.url.includes('你的网盘地址') || mirror.url.includes('建设中') ? (
              <span class="download-btn" style="background: #2a2a3e; color: #666; cursor: default;">
                🚧 建设中…
              </span>
            ) : (
              <a href={mirror.url} class="download-btn" target="_blank">
                ⬇️ 下载镜像
              </a>
            )}
          </div>
        ))}
      </div>

      <div class="empty" id="emptyState" style="display: none;">
        没有找到匹配的镜像，请尝试其他筛选条件
      </div>

      <footer>
        <p>🦞 Powered by <a href="https://openclaw.ai" target="_blank">OpenClaw</a></p>
        <p>📂 <a href="https://github.com/hagenmagpie/openclaws" target="_blank">GitHub</a></p>
      </footer>
    </div>

    <script>
      const osFilter = document.getElementById('osFilter');
      const archFilter = document.getElementById('archFilter');
      const scenarioFilter = document.getElementById('scenarioFilter');
      const cards = document.querySelectorAll('.card');
      const emptyState = document.getElementById('emptyState');

      const scenarioSkills: Record<string, string[]> = {
        'smart-home': ['homeassistant', 'nodes'],
        'office': ['feishu-doc', 'feishu-wiki', 'feishu-drive'],
        'automation': ['cron', 'weather', 'memory'],
        'iot': ['nodes', 'homeassistant', 'weather'],
        'monitor': ['nodes', 'camera'],
      };

      function filterCards() {
        const os = osFilter.value;
        const arch = archFilter.value;
        const scenario = scenarioFilter.value;
        
        let visibleCount = 0;
        
        cards.forEach(card => {
          const cardOs = card.getAttribute('data-os');
          const cardArch = card.getAttribute('data-arch');
          const cardSkills = card.getAttribute('data-skills') || '';
          
          let show = true;
          
          if (os && cardOs !== os) show = false;
          if (arch && cardArch !== arch) show = false;
          
          if (scenario) {
            const neededSkills = scenarioSkills[scenario] || [];
            const hasSkill = neededSkills.some(s => cardSkills.includes(s));
            if (!hasSkill) show = false;
          }
          
          (card as HTMLElement).style.display = show ? 'block' : 'none';
          if (show) visibleCount++;
        });
        
        emptyState.style.display = visibleCount === 0 ? 'block' : 'none';
      }

      osFilter.addEventListener('change', filterCards);
      archFilter.addEventListener('change', filterCards);
      scenarioFilter.addEventListener('change', filterCards);

      // AI News Loading (from static JSON)
      async function fetchAINews() {
        const news = { latest: [], hot: [], updated: null };
        
        try {
          const response = await fetch('/openclaws/data/ai-news.json');
          const data = await response.json();
          news.latest = data.latest || [];
          news.hot = data.hot || [];
          news.updated = data.updated;
        } catch (error) {
          console.error('Failed to load news:', error);
        }
        
        return news;
      }

      function formatDate(isoString) {
        if (!isoString) return '';
        const date = new Date(isoString);
        return date.toLocaleString('zh-CN', { 
          month: 'numeric', 
          day: 'numeric', 
          hour: '2-digit', 
          minute: '2-digit' 
        });
      }

      function formatNewsTime(timestamp) {
        const date = new Date(timestamp);
        return date.toLocaleString('zh-CN', { 
          month: 'numeric', 
          day: 'numeric',
          hour: '2-digit',
          minute: '2-digit'
        });
      }

      function renderNewsCard(item, type) {
        return `
          <a href="${item.url}" target="_blank" class="news-card ${type}">
            <div class="news-title">${item.title}</div>
            <div class="news-meta">
              <span class="news-source">${item.source}</span>
              <span class="news-time">${formatNewsTime(item.time)}${item.score ? ' · ' + item.score + ' ▲' : ''}</span>
            </div>
          </a>
        `;
      }

      async function loadNews() {
        const latestContainer = document.getElementById('latestNews');
        const hotContainer = document.getElementById('hotNews');
        const updatedTime = document.getElementById('updatedTime');
        
        if (latestContainer) latestContainer.innerHTML = '<div class="news-loading">加载中...</div>';
        if (hotContainer) hotContainer.innerHTML = '<div class="news-loading">加载中...</div>';
        
        const news = await fetchAINews();
        
        // Update timestamp
        if (updatedTime && news.updated) {
          updatedTime.textContent = '更新时间: ' + formatDate(news.updated);
        }
        
        if (latestContainer) {
          if (news.latest.length > 0) {
            latestContainer.innerHTML = news.latest.slice(0, 10).map((item) => renderNewsCard(item, 'latest')).join('');
          } else {
            latestContainer.innerHTML = '<div class="news-error">暂无最新资讯</div>';
          }
        }
        
        if (hotContainer) {
          if (news.hot.length > 0) {
            hotContainer.innerHTML = news.hot.slice(0, 10).map((item) => renderNewsCard(item, 'hot')).join('');
          } else {
            hotContainer.innerHTML = '<div class="news-error">暂无热门资讯</div>';
          }
        }
      }

      // Load news on page load
      loadNews();
    </script>
  </body>
</html>
